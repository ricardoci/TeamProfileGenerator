const Employee = require('../lib/Employee');

describe('Employee', () => {
  test('should throw error if render() is called', () => {
    
    class Employee{
        constructor(){
            this.name,
            this.id,
            this.email
    
        }
    }
    

  });
});